import { UserCog } from 'lucide-react';

/** The Service Accounts section's own glyph, static where a user gets initials. */
export function ServiceAccountIcon() {
  return <UserCog size={28} strokeWidth={1.5} aria-hidden />;
}
