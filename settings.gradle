plugins {
    id 'com.enonic.xp.settings' version '4.1.0'
}

include 'testing'

if ( hasProperty( 'env' ) ) {
    addBuild('../lib-admin-ui')
}

def addBuild( name )
{
    def dir = new File( (File) settingsDir, name )
    if (dir.directory) {
        includeBuild name
    }
}
